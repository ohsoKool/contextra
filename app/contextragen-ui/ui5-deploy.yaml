specVersion: "3.1"

metadata:
  name: contextragen.ui

type: application

builder:
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo

      configuration:
        archiveName: contextragen-ui
        relativePaths: true

        additionalFiles:
          - xs-app.json
